pipeline {
    agent any

    stages {
        stage('clone project') {
            steps {
                sh 'git clone https://github.com/mhassini/timesheet-devops'
            }
        }
       
        
    }
}
